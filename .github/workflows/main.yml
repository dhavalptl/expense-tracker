name: CI

on:
  push:
    branches:
    - master

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build
      env:
        DOCKER_HUB: ${{ secrets.DKR_NAME }}
        DOCKER_HUB_KEY: ${{ secrets.DKR_TKN }}
      run: |
        echo 'Docker login'
        docker login -u $DOCKER_HUB -p $DOCKER_HUB_KEY
        echo 'Running build.....'
        docker build -t dhavalptl/expense-tracker .
        echo 'Pushing image...'
        docker push dhavalptl/expense-tracker
        echo 'Done!!!!'
